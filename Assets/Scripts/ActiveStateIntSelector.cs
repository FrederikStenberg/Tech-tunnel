using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActiveStateIntSelector : StateMachineBehaviour {

    override public void OnStateEnter(Animator animator, AnimatorStateInfo stateInfo, int layerIndex)
    {
        //AgentSelector aSel = animator.gameObject.GetComponent<AgentSelector>();
        //aSel.startCour();
    }
}
